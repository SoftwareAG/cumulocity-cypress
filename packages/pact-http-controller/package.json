{
  "name": "cumulocity-cypress-ctrl",
  "description": "HTTP controller for Cumulocity pact recordings",
  "scripts": {
    "start": "cross-env NODE_NO_WARNINGS=1 npx tsx --env-file=.env src/startup.ts --config c8yctrl-dev.config.ts",
    "clean": "rimraf ./dist/",
    "build": "npm run clean && tsc --project tsconfig.package.json"
  },
  "author": "twi@softwareag.com",
  "license": "Apache-2.0"
}
